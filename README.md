# Event-Driven-Job-Scheduler

### About the project
this project is currently in development
currently is built while using fork and exec (might change to posix_spawn later on)

current project will run any command string by the user - meaning its not safe (yet)